# Unity Assets Folder

This tree represents the intended source layout. The repository is not considered a valid Unity project until the approved Unity version creates `Packages/` and `ProjectSettings/` and generates the required `.meta` files.

Codex must preserve the documentation and Git configuration when creating the Unity project.
