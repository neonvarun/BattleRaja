# Milestone 2 Report — Combat Laboratory

## Gate status

**Provisional pass.** The technical completion gate passed. Subjective combat feel,
damage tuning, touch ergonomics, formal performance profiling, and human review remain
open. M3 fighter-specific work is safe to start; no M3 kit was added here.

## Implemented

- Pure Domain combat contracts: typed entity IDs/factions, validated damage requests,
  health state, central damage pipeline, weapon definition validation, cooldown state,
  attack commands, projectile travel/lifetime/range simulation, and duplicate-hit
  tracking.
- Presentation combat bridge: configurable `ProjectileWeaponAsset`, shared attack
  input for mouse/gamepad/Android button, `CombatAttackController`, bounded projectile
  pool, explicit layer-filtered sphere casts, faction/self-hit/friendly-fire policy,
  and resettable `TrainingDummy`.
- Placeholder feedback: hit flash, pooled impact effect, and a feedback-ready sound
  hook surface without adding audio assets.
- MovementLab scene updated with a training dummy, combat systems, projectile/impact
  materials, serialized asset references, and attack control.

## Acceptance matrix

| Criterion | Evidence | Status |
|---|---|---|
| Movement remains functional | M1 regression tests included in current suites | Passed |
| Central health mutation path | `DamagePipeline` → `CombatDamageResolver` → `CombatHealth` | Passed |
| One playable projectile-to-damage loop | PlayMode projectile hit test; Android/Web builds | Passed |
| Explicit projectile speed/range/lifetime/radius/layer/despawn | `ProjectileWeaponDefinition` and `CombatProjectile` | Passed |
| Duplicate-hit and faction/self-hit rules | Domain tests and `ProjectileHitTracker` | Passed |
| Cooldown/fire-rate enforcement | Domain cooldown tests and attack controller | Passed |
| Pooling and dummy reset | PlayMode pool reuse and reset tests | Passed |
| Android smoke | M2 APK installed/launched on Lava API34 and Oppo API36 | Passed with warning |
| Web smoke | Local HTTP 200; Chrome 150 and Edge 150 Unity bootstrap DOM | Passed |
| Human combat/touch review | `Docs/HUMAN_REVIEW_BACKLOG.md` | Open |
| Formal performance capture | No profiler capture collected | Not run |

## Tests and builds

- EditMode: 15/15 passed in `Builds/M2/TestResults/editmode.xml`.
- PlayMode: 13/13 passed in `Builds/M2/TestResults/playmode.xml`.
- Android command: `pwsh -File Tools/Build/Android/build.ps1 -ProjectRoot . -UnityExe "C:\Program Files\Unity\Hub\Editor\6000.5.6f1\Editor\Unity.exe"`.
- Android artifact: `Builds/M2/Android/BattleRaja-M2.apk` (approximately 83.0 MB on disk).
- Web command: `pwsh -File Tools/Build/Web/build.ps1 -ProjectRoot . -UnityExe "C:\Program Files\Unity\Hub\Editor\6000.5.6f1\Editor\Unity.exe"`.
- Web artifact: `Builds/M2/Web/index.html` and `Builds/M2/Web/Build/` (approximately 113.0 MB reported by Unity).
- Web local serving: Python HTTP server on port 8002; `index.html` returned HTTP 200.
- Chrome/Edge headless DOM checks found Unity bootstrap content.
- ADB smoke: package `com.example.battleraja.m2` installed and launched on Lava
  `ST5GDW23LB004392` and Oppo `b60e53b3`; both reported zero fatal application
  exceptions and the Unity activity was resumed.

## Warnings and limitations

- Unity emits the known non-fatal Play Asset Delivery
  `AssetPackManager` `ClassNotFoundException` probe warning in development APK logs.
- Unity batch logs include licensing-handshake noise and obsolete API warnings for
  the existing build entrypoint APIs; the builds and tests still completed.
- No physical attack-button gesture, browser canvas focus-loss gesture, profiler
  capture, memory capture, or visual combat-feel review was performed.
- The Android application ID `com.example.battleraja.m2` is provisional and not a
  release/store identity.

## Human and external gates

- Human review debt is recorded in `Docs/HUMAN_REVIEW_BACKLOG.md`.
- Photon, PlayFab, analytics, hosting, store publication, signing, legal/privacy and
  cultural review remain untouched and are tracked in `Docs/EXTERNAL_SERVICE_GATES.md`.

## Checkpoint

Implementation checkpoint: `95aa3f9` (`milestone M2: combat laboratory`).

## Recommended next action

Have the owner play the M2 laboratory on both physical Android devices and desktop Web,
then approve or adjust the provisional damage/feel baseline before M3 Bijli work.
